spring:
  application:
    name: provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

dubbo:
  scan:
    base-packages: com.example
  protocol:
    name: dubbo
    port: -1
server:
  port: 10000
  servlet:
    context-path: /provider